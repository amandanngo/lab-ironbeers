
    {{#each allBeers}}
        <img height='100px' src="{{this.image_url}}" alt="beer pic"/> 
        <p>{{this.name}}</p>
        <p>{{this.description}}</p>
        <p>{{this.tagline}}</p>
    {{/each}}
